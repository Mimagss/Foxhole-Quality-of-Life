# General Information

Everything in this project is free to use.  
Share it, modify it, or do whatever you want with it — the developer does not care.

## Requirements

To run the scripts, you need:

- Python 3.8 or later  
  *(development was done with Python 3.14)*
- `pip`  
  *(required to install Python libraries)*

## Installation

Run the following command inside the `Foxhole-Quality-of-Life` folder:

```bash
pip install -r requirements.txt